function params(a, b) {
    if (a === b) {
        return 4 * a;
    } else {
        return a * b;
    }
}

const params = function(a, b) {
    if (a === b) {
        return 4 * a;
    } else {
        return a * b;
    }
};

const params = (a, b) => {
    return (a === b) ? 4 * a : a * b;
};
